cmake_minimum_required(VERSION 2.8)
project( all )
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")
find_package( OpenCV 3.2.0 EXACT REQUIRED )
add_executable( all all.cpp )
target_link_libraries( all ${OpenCV_LIBS} )
